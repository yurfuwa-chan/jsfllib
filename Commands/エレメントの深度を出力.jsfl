fl.outputPanel.clear();fl.trace("エレメントの深度を出力")var timeline = fl.getDocumentDOM().getTimeline();var layers = timeline.layers;layers.forEach(function(layer,l){	fl.trace(layer.name+" : "+l);	var frames = layer.frames;	frames.forEach(function(frame,f){		//キーフレーム以外は処理を続行しない		if(frame.startFrame != f)return;		//elements配列は深度の深いものから配列に格納されるため昇順にソート		var elements = frame.elements.sort(function(a,b){			return a.depth - b.depth		})		elements.forEach(function(element){//出力			fl.trace(" "+element.depth+" : "+element.name)		});	})});	