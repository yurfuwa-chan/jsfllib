var doc = fl.getDocumentDOM();var lib = doc.library;var items = lib.items;var uri = fl.browseForFileURL("open","座標を指定して読み込み");var rect,instance;doc.importFile(uri);instance = doc.selection[0];rect = prompt("x ,y",instance.x+","+instance.y).split(",");instance.x = Number(rect[0]);instance.y = Number(rect[1]);items.forEach(function(item,i){	if(!(item instanceof FolderItem)){			}})